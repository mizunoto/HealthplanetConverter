<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fragmentNavigation"
    app:startDestination="@id/loadFragment">

    <fragment
        android:id="@+id/loadFragment"
        android:name="com.mizunoto.hpconv.Load"
        android:label="@string/load_title"
        tools:layout="@layout/fragment_load" />
    <fragment
        android:id="@+id/loadByDateFragment"
        android:name="com.mizunoto.hpconv.LoadByDate"
        android:label="@string/load_by_date_title"
        tools:layout="@layout/fragment_load_by_date" />
    <fragment
        android:id="@+id/getTokenFragment"
        android:name="com.mizunoto.hpconv.GetToken"
        android:label="@string/get_token_title"
        tools:layout="@layout/fragment_get_token" />
    <fragment
        android:id="@+id/settingsFragment"
        android:name="com.mizunoto.hpconv.Settings"
        android:label="@string/settings_title"
        tools:layout="@layout/fragment_settings" />
</navigation>